To-do list for ranged_set
=========================

src/lib.rs
----------

 - Add `remove(...)` function
 - Add `take(...)` function

src/element/mod.rs
------------------

 - Make `merge(...)` return an `Option<_>`
